package com.interviewportal.interview.entity;

/** Seniority band the candidate interviewed for. Enum keeps the values consistent and filterable. */
public enum ExperienceLevel {
    FRESHER,
    JUNIOR,
    MID,
    SENIOR,
    STAFF,
    PRINCIPAL
}
