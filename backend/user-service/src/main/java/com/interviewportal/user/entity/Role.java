package com.interviewportal.user.entity;

/**
 * Application roles. Kept intentionally small (only what the product needs) to keep
 * authorisation rules easy to reason about.
 */
public enum Role {
    USER,
    ADMIN
}
